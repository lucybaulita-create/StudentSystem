# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections - use trust for local
local   all             all                                     trust

# IPv4 local connections use md5 password authentication
host    all             all             127.0.0.1/32            md5

# IPv6 local connections use md5 password authentication  
host    all             all             ::1/128                 md5

# All other connections use md5
host    all             all             0.0.0.0/0               md5

# Replication connections
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5


